# Copy to .env and fill in. Do not commit .env.
# Loaded automatically on server/mcp start (OS env still wins if set).

GOOGLE_MAPS_API_KEY=

# OpenAI-compatible LLM (Ollama local or Render private service).
# Empty LLM_BASE_URL disables /api/v1/llm/* (clients use template fallback).
LLM_BASE_URL=http://127.0.0.1:11434/v1
LLM_MODEL=gemma2:2b
LLM_API_KEY=

# CPU LLM on Render can exceed 60s — raise if needed.
# SERVER_WRITE_TIMEOUT_SECONDS=120
